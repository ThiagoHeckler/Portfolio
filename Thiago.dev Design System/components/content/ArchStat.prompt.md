A monospace stat with a muted caption, used across the infrastructure strip ("Onde isso roda / VPS").

```jsx
<ArchStat stat="1 vCPU · 4 GB" label="VPS única, com 2 GB de swap" />
<ArchStat stat="384 MB / app" label="limite de RAM por container" />
```
