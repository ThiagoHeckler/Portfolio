Monospace pill that labels which database a project uses. The blue/green pairing is a core brand signal.

```jsx
<Badge db="pg">PostgreSQL</Badge>
<Badge db="mongo">MongoDB</Badge>
```

`db`: `pg` (blue), `mongo` (green), `accent` (amber).
